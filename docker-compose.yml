services:
  evolvetrace:
    build: .
    ports:
      - "8001:8001"
    environment:
      - EVOLVETRACE_DB_PATH=/data/audit.db
      - EVOLVETRACE_HARNESS_DB_PATH=/data/harness.db
    volumes:
      - evolvetrace-data:/data
    restart: unless-stopped

volumes:
  evolvetrace-data:

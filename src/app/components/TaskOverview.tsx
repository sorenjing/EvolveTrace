import type { TaskContract } from "@/app/lib/harness-types";

export function TaskOverview({ task }: { task: TaskContract | null }) {
  if (!task) return <div className="rounded-xl border border-dashed border-zinc-300 p-6 text-sm text-zinc-500 dark:border-zinc-700">选择 Task 或 Unbound Run 开始审查。</div>;
  return <section><p className="text-[11px] font-semibold uppercase tracking-[0.2em] text-teal-600">Task Contract</p><h2 className="mt-1 text-xl font-semibold">{task.title}</h2><p className="mt-2 text-sm leading-6 text-zinc-600 dark:text-zinc-300">{task.goal}</p><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-zinc-500">状态</dt><dd>{task.status}</dd></div><div><dt className="text-zinc-500">目标仓库</dt><dd>{task.target_repositories.join(", ")}</dd></div></dl><div className="mt-4 space-y-2">{task.acceptance_criteria.map((item, index) => typeof item === "string" ? <p key={index} className="rounded bg-zinc-100 p-2 text-xs dark:bg-zinc-900">旧版人工验收：{item}</p> : <p key={item.criterion_id} className="rounded bg-zinc-100 p-2 text-xs dark:bg-zinc-900"><b>{item.required ? "必需" : "可选"}</b> · {item.description} · {item.type}</p>)}</div>{task.open_questions.length > 0 && <p className="mt-4 rounded bg-amber-50 p-3 text-sm text-amber-800 dark:bg-amber-950/40 dark:text-amber-200">待确认：{task.open_questions.join("；")}</p>}</section>;
}
